import React from 'react';
import UserProfile from '@shared/components/profile/UserProfile';

const ParentProfile: React.FC = () => {
  return <UserProfile role="parent" roleLabel="Phụ huynh" />;
};

export default ParentProfile;
